# Private working material. Never published.
/submit/
/.hq/
/archive/

# Primary captures. Kept on disk as the evidence every number traces to, never
# republished: 404 files including third-party page captures and six PDFs whose
# terms do not grant redistribution.
/docs/research/raw/

# Build output and dependencies
node_modules/
.next/
out/
dist/
*.tsbuildinfo

# Local state
.env
.env.*
!.env.example
*.log
/data/
*.db
*.db-wal
*.db-shm

# Render outputs
DEMO-*.mp4
upload-*/
.claude/
lib/__gate/
/CONTENT-*.html

# [doc 16] Altana agent keys are secrets. .hq is already ignored above, this is explicit.
/.hq/altana/
# Next 16 agentRules writes these at the project root on dev/build. Generated, never committed.
/AGENTS.md
/CLAUDE.md
