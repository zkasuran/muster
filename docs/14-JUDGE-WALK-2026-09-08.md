# Judge walk of muster.zkasuran.dev, 2026-09-08

Read-only walk of the live site as a first-time judge, scored against the three quoted criteria:
Functionality (land, find by category, understand, activate, no dead end), Data Quality (real-time,
accurate, informed hiring call) and Agent Diversity (four categories at equal depth).

Method: curl of every page with desktop and iPhone user agents, HTML stripped to visible text, every
href on the landing, the four shelves and one agent page fetched, the hire endpoint probed three ways,
chain facts re-derived over JSON-RPC against bsc-rpc.publicnode.com, x402 field names checked against
coinbase/x402 `specs/x402-specification-v2.md` and `specs/transports-v2/http.md`. All times UTC.

The site changed under the walk. At 13:09 the landing read "Registered on chain 336,339 at block
120,208,122" and "Payable by a stranger 5". A sweep started around 13:15. From 13:19 to at least 13:26
every rung above "declared" was gone site-wide. Both states are recorded below because a judge could
land in either.

## Findings

| # | URL | What I saw (exact text) | Costs | Severity | Fix in one sentence |
|---|---|---|---|---|---|
| 1 | `/`, `/status`, `/agent/900000001`, `/compare?ids=900000001,322885,259573` | 13:09: landing "Payable by a stranger 5", agent "5/6payable", compare Hire "open and hire". 13:21:50: landing "Payable by a stranger 0", ladder "3/6reachable 0, 4/6probed 0, 5/6payable 0", every shelf card "2/6declared", status "Last sweep unknown still running or never run", agent "2/6declared" while its own Probe history still reads "pass G3-endpoint-hygiene HTTP 402 402 with requirements 29m ago", compare Hire "not payable, nothing to hire yet" for all three columns, shelves "payable or better: none yet". Unchanged at 13:26:32. | Functionality, Data Quality | blocker | Derive the rung from the persisted probe records (or keep the last completed sweep's rungs until the new one finishes) so a running sweep can never demote a row or hide the hire path. |
| 2 | `/agent/900000001` | "What a buyer can act on: Price 0.02 USD1, Payment rail eip3009, Paid to 0xcd10D447...4c64" then "Endpoints https://muster.zkasuran.dev/api/agent/health-factor" as plain text (0 anchors in that block). No button, no steps, no link to `?preview=1`, no sample request. The compare page's only hire control, "open and hire", links back to this page. | Functionality | blocker | Add a "Hire this agent" block with the preview link, a copyable request that returns the 402, the human price and the exact pay-to, network and asset a buyer must sign for. |
| 3 | `/agent/900000001` (also 900000002, 900000003, 900000004) | Header "agent id 900000001 on BNB Smart Chain operated by us"; "On chain: Owner 0xcd10d447...4c64, Payout wallet 0xcd10d447...4c64, First seen at block unknown"; link "View on BscScan" to `bscscan.com/token/0x8004A169...a432?a=900000001`. Verified on chain: `ownerOf(900000001)` on 0x8004A169FB4a3325136EB29fA0ceB6D2e539a432 reverts with ERC721NonexistentToken (`0x7e273289...35a4e901`), same for 900000002 to 900000004, while `ownerOf(6428)` and `ownerOf(1)` resolve. | Data Quality | major | Register the four first-party agents on the registry, or relabel the block "not registered on chain, operated by Muster" and drop the BscScan link and the "on BNB Smart Chain" phrasing until they are. |
| 4 | `GET /api/agent/health-factor` (and `?account=0xed87...B0bE`) | HTTP/2 402, headers `content-type: application/json` only, no `PAYMENT-REQUIRED` header. Body: `{"x402Version":2,"accepts":[{"scheme":"eip3009","network":"eip155:56","asset":"0x8d0D000Ee44948FC98c9B98A4FA4921476f08B0d","maxAmountRequired":"20000000000000000","payTo":"0xcd10D44703D6989290E0A8219f345fA0a5BF4c64","resource":"https://muster.zkasuran.dev/api/agent/health-factor",...,"extra":{"name":"World Liberty Financial USD","version":"1"}}],"error":"PAYMENT-SIGNATURE header is required"}`. Spec transports-v2/http.md: "HTTP 402 status code with `PAYMENT-REQUIRED` header", "Base64-encoded `PaymentRequired` schema in header". Spec v2 core: "`amount` string Required"; `maxAmountRequired` does not appear in v2. Tator's live 402 shows the real shape (`payment-required:` header, `"scheme":"exact"`, `"amount":"200000"`). Price is human readable only in `?preview=1` (`"human":"0.02 USD1"`) and on the agent page, not in the 402 itself. | Functionality (machine hire path) | major | Emit the base64 `PAYMENT-REQUIRED` header, rename `maxAmountRequired` to `amount`, use scheme `exact` with the eip3009 payload underneath and lift `resource` to the top-level object, keeping the JSON body as a courtesy copy. |
| 5 | `/shelf/yield`, `/agent/6428` | First row of Yield: "Tator Trader id 6428 5/6payable ... Price not quoted, Rail none, Endpoints 4 ... claims x402, unverified". Agent page: "Paid to no payout address published" while the On chain block says "Payout wallet 0xc5ca25f9...e1b0". Its own description says "$0.20 per request via x402 micropayments". I fetched its 402: 15 accept options (eip155:8453, 1, 42161, 10, 137, 43114, 130, 59144, 4326, 146, 999, 57073, 143, 2741 and solana), amount 200000 USDC, and no eip155:56 at all. | Data Quality, Agent Diversity | major | Parse the accepts array the probe already received and show price, asset and networks; mark rows with no eip155:56 option "payable, not on BSC" and rank them below BSC-payable rows on a BSC directory. |
